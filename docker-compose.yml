services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: smpte-dropframe-checkstation:local
    ports:
      - "8080:80"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
